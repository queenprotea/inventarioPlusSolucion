<UserControl x:Class="ClienteInventarioPlus.Vistas.ConsultarProductoEmpleado"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClienteInventarioPlus.Vistas"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1170"
             Background="WhiteSmoke"
             Loaded="UserControl_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Consultar Productos" 
                   FontSize="28" FontWeight="Bold" 
                   Foreground="#333" Margin="0,0,0,15"/>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="TxtBusqueda" Grid.Column="0" Padding="5" FontSize="14" VerticalContentAlignment="Center"/>

            <ComboBox x:Name="CmbFiltro" Grid.Column="1" Margin="10,0" Padding="5" VerticalContentAlignment="Center" MinWidth="150">
                <ComboBoxItem Content="Buscar por Nombre"/>
                <ComboBoxItem Content="Buscar por Código"/>
                <ComboBoxItem Content="Buscar por Categoría"/>
            </ComboBox>

            <Button x:Name="BtnBuscar" Grid.Column="2" Content="Buscar" Padding="20,5" Click="BtnBuscar_Click"/>
        </Grid>

        <DataGrid Grid.Row="2" x:Name="DgProductos" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True" AlternatingRowBackground="LightGray">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="2*"/>
                <DataGridTextColumn Header="Categoría" Binding="{Binding Categoria.Nombre}" Width="1*"/>
                <DataGridTextColumn Header="Precio Venta" Binding="{Binding PrecioVenta, StringFormat={}{0:C}}" Width="Auto"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="Auto"/>
                <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="3*"/>
                <DataGridTextColumn Header="Proveedor" Binding="{Binding Proveedor.Nombre}" Width="1*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
            <Button Content="&lt;&lt;" Margin="2"/>
            <Button Content="1" Margin="2" FontWeight="Bold"/>
            <Button Content="2" Margin="2"/>
            <Button Content="3" Margin="2"/>
            <TextBlock Text="..." Margin="5,0" VerticalAlignment="Center"/>
            <Button Content="&gt;&gt;" Margin="2"/>
        </StackPanel>

    </Grid>
</UserControl>