<UserControl x:Class="ClienteInventarioPlus.Vistas.ModificarUsuario"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ClienteInventarioPlus.Vistas"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="{StaticResource BackgroundColor}">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0"
                   Text="Modificar Usuario"
                   Style="{StaticResource TitleText}"
                   Margin="0,0,0,32"/>

        <!-- Formulario en Card con ScrollViewer -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <Border Style="{StaticResource Card}"
                    MaxWidth="600"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Margin="0,0,0,24">
                <StackPanel>
                    <!-- Campo Nombre -->
                    <Label Content="Nombre"
                           Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="tbNombre"
                             Style="{StaticResource ModernTextBox}"
                             Height="40"
                             Margin="0,0,0,4"/>
                    <TextBlock x:Name="tblockErrorNombre"
                               Style="{StaticResource ErrorText}"
                               Margin="0,0,0,16"/>

                    <!-- Campo Nombre de Usuario -->
                    <Label Content="Nombre de Usuario"
                           Style="{StaticResource FormLabel}"
                           Margin="0,8,0,0"/>
                    <TextBox x:Name="tbUsername"
                             Style="{StaticResource ModernTextBox}"
                             Height="40"
                             Margin="0,0,0,4"/>
                    <TextBlock x:Name="tblockErrorUserName"
                               Style="{StaticResource ErrorText}"
                               Margin="0,0,0,16"/>

                    <!-- Campo Contraseña -->
                    <Label Content="Contraseña"
                           Style="{StaticResource FormLabel}"
                           Margin="0,8,0,0"/>
                    <TextBox x:Name="tbPassword"
                             Style="{StaticResource ModernTextBox}"
                             Height="40"
                             Margin="0,0,0,4"/>
                    <TextBlock x:Name="tblockErrorPassword"
                               Style="{StaticResource ErrorText}"
                               Margin="0,0,0,16"/>

                    <!-- Campo Rol -->
                    <Label Content="Rol"
                           Style="{StaticResource FormLabel}"
                           Margin="0,8,0,0"/>
                    <StackPanel Orientation="Horizontal" 
                                Margin="0,8,0,4">
                        <RadioButton x:Name="rbAdministrador" 
                                     Content="Administrador" 
                                     GroupName="Roles" 
                                     FontSize="14"
                                     Margin="0,0,32,0"/>
                        <RadioButton x:Name="rbEmpleado" 
                                     Content="Empleado" 
                                     GroupName="Roles" 
                                     FontSize="14"/>
                    </StackPanel>
                    <TextBlock x:Name="tblockErrorRol"
                               Style="{StaticResource ErrorText}"
                               Margin="0,0,0,24"/>

                    <!-- Botones -->
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                Margin="0,16,0,0">
                        <Button Content="Cancelar"
                                Style="{StaticResource SecondaryButton}"
                                Margin="0,0,12,0"
                                Click="btnCancelar_Click"/>
                        <Button Content="Modificar Usuario"
                                Style="{StaticResource PrimaryButton}"
                                Click="btnModificarUsuario_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
