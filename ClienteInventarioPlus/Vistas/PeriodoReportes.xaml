<UserControl x:Class="ClienteInventarioPlus.Vistas.SeleccionarPeriodoVista"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1170"
             Background="{StaticResource BackgroundColor}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0"
                   Text="Seleccionar Periodo del Reporte"
                   Style="{StaticResource TitleText}"
                   Margin="32,32,32,32"
                   TextAlignment="Center"/>

        <!-- Contenido -->
        <Border Grid.Row="1"
                Style="{StaticResource Card}"
                MaxWidth="500"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Margin="32">
            <StackPanel>
                <TextBlock Text="Selecciona el periodo de movimientos que deseas reportar:" 
                           Style="{StaticResource BodyText}"
                           TextAlignment="Center"
                           Margin="0,0,0,32"
                           TextWrapping="Wrap"/>

                <!-- Fecha Inicial -->
                <Label Content="Fecha inicial del reporte" 
                       Style="{StaticResource FormLabel}"/>
                <DatePicker x:Name="DpFechaInicio" 
                            Height="40"
                            FontSize="14"
                            Margin="0,0,0,24"/>

                <!-- Fecha Final -->
                <Label Content="Fecha final del reporte" 
                       Style="{StaticResource FormLabel}"
                       Margin="0,8,0,0"/>
                <DatePicker x:Name="DpFechaFin" 
                            Height="40"
                            FontSize="14"
                            Margin="0,0,0,32"/>

                <!-- Botones -->
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Right">
                    <Button x:Name="BtnCancelar" 
                            Content="Cancelar"
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,12,0"
                            Click="BtnCancelar_Click"/>
                    <Button x:Name="BtnGenerar" 
                            Content="Generar Reporte" 
                            Style="{StaticResource PrimaryButton}"
                            Click="BtnGenerar_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
