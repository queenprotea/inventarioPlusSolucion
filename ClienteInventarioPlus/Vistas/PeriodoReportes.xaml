<UserControl x:Class="ClienteInventarioPlus.Vistas.SeleccionarPeriodoVista"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1170"
             Background="WhiteSmoke">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

            <TextBlock Text="Selecciona el periodo de movimientos que deseas reportar:" 
                       FontSize="20" FontWeight="SemiBold" 
                       Foreground="#333" Margin="0,0,0,30" TextAlignment="Center"/>

            <Border Background="White" CornerRadius="8" Padding="20" MinWidth="400" Margin="0,0,0,20" BorderBrush="LightGray" BorderThickness="1">
                <StackPanel>
                    <Label Content="Fecha inicial del reporte" FontSize="16" Foreground="Gray"/>
                    <DatePicker x:Name="DpFechaInicio" FontSize="18" Padding="5" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="White" CornerRadius="8" Padding="20" MinWidth="400">
                <StackPanel>
                    <Label Content="Fecha final del reporte" FontSize="16" Foreground="Gray"/>
                    <DatePicker x:Name="DpFechaFin" FontSize="18" Padding="5" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,40,0,0">
                <Button x:Name="BtnGenerar" Content="Generar Reporte" Padding="20,10" 
                        Background="Black" Foreground="White" FontWeight="Bold"
                        Click="BtnGenerar_Click"/>
                <Button x:Name="BtnCancelar" Content="Cancelar" Margin="15,0,0,0" Padding="20,10" Click="BtnCancelar_Click"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</UserControl>